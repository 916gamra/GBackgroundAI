# 🔍 تقرير مراجعة GBackgroundAI — Beast v15

> تقرير مراجعة شامل + ما تم إصلاحه فعلياً في هذا الفرع + خارطة تطوير مقترحة.
> كل بند أدناه **تم تنفيذه والتحقق منه** (`tsc` نظيف، `lint:strict` = 0 أخطاء، `npm test` = 69 تأكيداً، `npm run smoke` ينجح، والبناء يعمل).

---

## 1) كيف تمت المراجعة

1. قراءة نواة التطبيق كاملة: `App.tsx` (1800+ سطر)، `aiService.ts`، `streamEngine.ts`، `AgentOrchestrator.ts`، محولات المزودين، `GSoulEngine.ts`، `agentTools.ts` (4200 سطر)، وصفحات الإعدادات.
2. تشغيل فاحص **unused** صارم (`--noUnusedLocals --noUnusedParameters`) لكشف الإعدادات والـ props والدوال الميتة → كشف **87** موضع كود ميت.
3. رسم خريطة استيراد (reachability graph) للعثور على الملفات المعزولة (لا يستوردها أحد).
4. تدقيق الاعتماديات مقابل الاستخدام الفعلي في `src/`.
5. مراجعة أمنية: تقييم ديناميكي، `dangerouslySetInnerHTML`، عزل المعاينة، تسريب مفاتيح، ما يُخزَّن في `localStorage`.
6. قياس حجم الحزمة قبل/بعد.
7. كتابة اختبارات منطق + اختبار تركيب (mount) للتأكد أن الإصلاحات ليست شكلية.

**النتيجة التشخيصية الأهم:** التطبيق كان «واجهات أنيقة موصولة بأزرار لا تفعل شيئاً» — عشرات الإعدادات تُعرض وتُحفظ لكن لا يقرأها منطق الطلب، وأخطاء المزود تُرفض بصمت، وحجم `max_tokens` كان يُرسل خاطئاً بطريقة تُفشل معظم الطلبات.

---

## 2) أعطال حقيقية كانت تكسر الاستخدام اليومي (تم إصلاحها)

| # | المشكلة كما كانت | الإصلاح |
|:-|:--|:--|
| 1 | **`max_tokens` = حجم نافذة السياق** (مثلاً `2097152` لـ Gemini Pro، `131072` لـ Qwen). أي مزود يرفضها برسالة 400 → الردود تفشل أو تعتمد على تسامح البوابة. حقل `maxTok` في الإعدادات كان **لا يُقرأ إطلاقاً**. | `mo` لكل نموذج في `MODELS` + `resolveMaxOutputTokens(model, userOverride)` بحدّ Floor = 512، وربطه في `buildCtx` وحلقة الـ Agent والمسار القياسي. |
| 2 | **إعداد «عدد رسائل السياق» (`ctx`) ميت**: `buildCtx` كان يقطّع `slice(-20)` ثابتاً دائماً. | `buildCtx` صار `slice(-(ctx+1))` مع اختبار صريح في الاختبارات. |
| 3 | **تلخيص تلقائي ميت**: `autoSum` / `sumThreshold` / `sumKeep` كانت «نصاً توضيحياً» في الإعدادات فقط. | `services/summarizeHistory.ts`: تلخيص تدريجي (rolling) يحقن الملخص السابق، بديل محلي عند غياب المفتاح، حارس تكرار، ولا يعمل كل رسالة بل عند تراكم 6+ رسائل جديدة. |
| 4 | **زر Web Search في شريط الإدخال لا يفعل شيئاً** (يبدّل الحالة فقط). | ينفّذ البحث ويحقن النتائج كسياق، في وضع Agent والوضع العادي، ويوسم النتائج بوضوح أنها موسوعية عند غياب مفتاح Serper. |
| 5 | **TTS كان يتجاهل الصوت والسرعة المختارين** (`voice: ''`, `rate: 1`). | `speakText(text, onEnd, onError, {voiceName, rate, pitch, lang})` + اختيار الصوت من قائمة فعلية في الإعدادات. |
| 6 | **`make_chart` يُعلَن للنموذج لكن المخطط لا يُعرض أبداً** في الوضع العادي، ولا يُستخرج في وضع Agent. | `pendingChartsRef` + `images` على الرسالة + عرض PNG في `AgentBubble` مع حفظ/فتح في تبويب. |
| 7 | **وضع Agent يفقد كل التحسينات**: لا Artifacts، لا TTS، لا GSoul، ولا تلخيص، ورسالة مكررة. | مسار توحيد واحد `finalizeReply({mode:'replace-last'})` يمر على كل الخطوات للوضعين. |
| 8 | **«إيقاف» لا يوقف حلقة الأدوات**، وأدوات معلّقة بلا مهلة تُجمّد الجلسة للأبد. | `orchestrator` يفحص `signal.aborted` قبل كل تكرار وبعد تنفيذ الأدوات + `fetchT()` بمهلة 20s تلغي الطلب عند الإيقاف (طُبّقت على 21 موضع fetch). |
| 9 | **قصّ مخرجات الأدوات بلا إعلان** → النموذج «يخترع» تكملة المخرج المقصوص. | رسالة `[output truncated: N chars total]` مرئية للمستخدم وموثقة للنموذج. |
| 10 | **أخطاء المزود خام**: `[HTTP 401] {"json":{…}}` + فقاعة فارغة عند الردود غير المتدفقة، و404 صامتة عند غياب المفتاح. | `streamEngine` يستخرج `error.message`، يضيف تلميحاً حسب الحالة، يقبل JSON غير متدفق، ويحوّل `TypeError` إلى رسالة CORS/إنترنت مفهومة. |
| 11 | **`GSoulEngine.clearWorking()` كان يمسح `sessionStorage` كاملاً** — يدمّر بيانات مكوّنات أخرى. `getRecentEpisodes` يرتّب حسب `toSorted(reverse id)` لا حسب الوقت، والذاكرة لا تُكتب إلا عند استخدام أداة `remember`. | مسح مفاتيح `gsoul_working_*` فقط، ترتيب زمني صحيح بحد أقصى، تسجيل كل تفاعل/أداة/خطأ، واسترجاع الذاكرة في كل طلب. |
| 12 | **حذف آخر محادثة ينشئ محادثتين فارغتين**، والإرسال من شاشة الترحيب مع `overrideSessionId` كان يكتب في محادثة غير نشطة ثم ينشئ واحدة فارغة. | منطق نظيف: إعادة استخدام الفارغة الحالية أو إنشاء واحدة فقط عند الحاجة، ودعم `overrideSessionId` في `appendToSession`. |
| 13 | **النص لا يُحدَّد ولا يُنسخ**: كان `select-none` على `<body>` في `index.html`. | إزالته + `.markdown-body { user-select: text }` مع إبقاء `select-none` على عناصر الواجهة فقط. |
| 14 | **زر البحث في الـ Header لا يفتتح شيئاً**، وصفحة المحادثة بلا حالة فارغة. | شريط بحث فعلي مع تظليل المطابقات، Esc/⌘F، تعطيل في شاشة الترحيب، واقتراحات بداية للمحادثة الفارغة. |
| 15 | **`ModelPickerModal` يعرض قائمة فارغة** عندما يكون الموديل `auto` والنموذج الافتراضي للمزود غير مُعَدد — وكان `onSwitchProvider` مُستلماً ولا يُستدعى. | عرض كل النماذج المُعدّة في هذه الحالة + تبديل المزود تلقائياً عند اختيار نموذج من مزود آخر. |
| 16 | **امتلاء التخزين يُفقد أحدث الرسائل بصمت** (`console.warn` فقط، مع 600KB+ من الردود لكل جلسة). | `services/safeStorage.ts`: تقليم تلقائي للأقدم + إعادة محاولة + شريط تنبيه قابل للإغلاق. |
| 17 | **مرفقات الصور تتحول لنص فاسد** (كانت تُقرأ بـ `readAsText`)، وملف 5MB يُتجاهل بصمت، ونص ضخم يُرسل كاملاً للنموذج. | الصور → Data URL في الـ Artifacts، اقتصاص 1.5MB مع ملاحظة واضحة، تنبيه للملفات المتخطّاة. |
| 18 | **Gemini adapter كان يتجاهل `baseUrl` المخصص**، ومنطق «التحقق من المفاتيح» يستورد نسخة ثانية من جدول المزودين. | احترام الـ baseUrl، ومصدر واحد للحقيقة (`DEFAULT_CHAT_ENDPOINT`). |

**إصلاح توثيقي مهم:** `.env.example` كان يحدد `GEMINI_API_KEY` بدون بادئة `VITE_`، أي أنه **لا يمكن الوصول إليه من المتصفح إطلاقاً**. صار `VITE_GEMINI_API_KEY` + دعم احتياطي فعلي في `App.tsx`.

---

## 3) الأمانة مع النموذج: سياسة ثقة الأدوات

من أصل 60 أداة، **28 أداة كانت تعيد JSON ملفقاً ثابتاً** وتُعلن للنموذج على أنها قدرات حقيقية:

`modbus_titan`, `modbus_master_pro`, `plc_unified_suite`, `device_hardware_overlord`, `termux_root_executor`, `device_security_suite`, `termux_forensics`, `voice_cloner`, `voice_changer`, `suno_generate`, `elevenlabs_clone`, `god_tier_ai_music`, `ai_video_gen`, `agent_swarm`, `agent_superintelligence_swarm`, `ben10_consciousness_core`, `ai_porn_generator`, `minecraft_liquid_control`, `live_data_bridge`, `system_monitor`, `memory_uploader`, `context_analyzer`, `data_analyst`, `pdf_analyzer`, `image_analysis`, `web_search` …

كان النموذج يكتب مثلاً «تم قراءة 18006 خطوة من PLC بنجاح» من بيانات مُختلَقة.

**الحل المنفذ** (`services/toolPolicy.ts` + `agentTools.ts` + `ToolsSettings.tsx`):

- تصنيف صريح: `REAL` / `HYBRID` / `SIMULATED` + أدوات مقيدة بمفتاح (`KEY_GATED`).
- الأدوات المحاكة **معطلة افتراضياً** ولا تُرسل للنموذج؛ تفعيلها يدوي من الإعدادات مع شارة `⚠ SIMULATED`.
- كل نتيجة محاكاة تُوسم `⚠ [SIMULATED DEMO TOOL]`، والـ system prompt يمنع تقديمها كحقيقة.
- الأدوات التي تحتاج مفتاحاً تُستبعد إن لم يكن موجوداً.
- «Enable all» في الإعدادات لم تعد تفعّل المحاكاة.
- عدد الأدوات المٌعلَنة افتراضياً: **60 → ~25**، والاختبار يثبت ذلك.
- الأدوات الوصفية (`fetch_file_to_code`, `run_code`…) لم تعد تُعلَن لأنها بلا تنفيذ.

> إن أردت إرجاع أي أداة للتجربة: الإعدادات → AI Tools → فعّلها، وستبقى موسومة كمحاكاة.

---

## 4) الأداء والحزمة

| المقياس | قبل | بعد |
|:--|--:|--:|
| `dist/assets/index-*.js` (الدخول) | **2,899.86 kB** (gzip 843.61) | **322.85 kB** (gzip 101.65) |
| `vendor-chart` | داخل الحزمة الرئيسية | chunk منفصل يُحمَّل عند أول مخطط فقط |
| صفحات الإعدادات/التوثيق | في الحزمة الرئيسية | `React.lazy` + `Suspense` |
| `highlight.js` | كل اللغات (~190+ لغة) | `lib/common` |
| تصدير المشروع | `jszip` + `file-saver` في الحزمة الرئيسية | استيراد ديناميكي عند الطلب |

أهم سبب للتضخم كان **تسريب المصدر إلى الحزمة**: `import.meta.glob('src/**/*', {query:'?raw', eager:true})` كان يضمن كل ملفات `src` كنص داخل الجافاسكربت بحجة «سياق مساحة العمل للمساعد» — أي أن الكود المصدري الكامل (1.7MB) كان يُحمَّل في كل زيارة. المساحة صارت تُزامَن وقت التشغيل عبر `projectMemory.syncWorkspace()` من الحالة الفعلية، والأداة تقرأ من الذاكرة لا من `fetch('/src/…')` الذي لا يعمل في الإنتاج.

كذلك: إزالة `express`, `dotenv`, `@google/genai`, `recharts`, `motion`, `framer-motion`, `framer-motion-3d`, `lz-string` من الاعتماديات (كانت مصدر معظم ثقل `vendor` وجودة بناء هشّة)، مع إبقاء `chart.js` (يُستخدم فعلاً) و`@capacitor/*`.

---

## 5) ملاحظة أمنية صريحة (مهمة قبل أي نشر عام)

- **المفاتيح في `localStorage`**: نموذج SPA بدون خادم؛ هذا مقبول للاستخدام الشخصي/التجريبي، **غير مقبول** لمفاتيح إنتاجية أو جهاز مشترك. لا تضع مفتاحاً في `.env` ثم تنشر الحزمة: `VITE_*` تُدمج في الجافاسكربت وتكون مرئية للجميع.
- **`math_eval`** يستخدم regex whitelist ثم `new Function()`. مقيّد بحروف الأرقام والعمليات، لكنه يبقى تقييماً ديناميكياً؛ المُدخلات تأتي من مخرجات النموذج (قابلة للـ prompt injection).
- **صندوق JS**: الـ Worker معزول عن الـ DOM لكنه **نفس الأصل** ويستطيع `fetch`/قراءة `localStorage`. للعزل الحقيقي على استضافة عامة: أضف CSP صارمة + `worker-src blob:` على مستوى السيرفر (أضفت `sandbox:allow-scripts` في الـ worker لأن `type:'module'` كان يمنع تشغيل بعض الشيفرات).
- **معاينة HTML**: `iframe sandbox="allow-scripts allow-forms allow-popups allow-modals"` — إزالة `allow-same-origin` صحيحة؛ لا تُعِدها لتفادي سرقة الجلسة.
- **`allowMixedContent` + `android:usesCleartextTraffic` في Capacitor**: ضروري للتواصل مع Termux/PLC عبر `http://` في شبكة محلية؛ يجب أن يبقى خارج أي بناء للشبكات العامة.
- **Prompt injection من نتائج الويب/الملفات** خطر قائم (النموذج يقرر استدعاء أدوات تنفّذ شيفرة). الحد الأدنى المنفَّذ الآن: تقييد الأدوات، وسم المحاكاة، ومهلة للطلبات، وقصّ المخرجات. الخطوة التالية المقترحة في §7 (تأكيد بشري قبل الأدوات المؤثرة).

---

## 6) التنظيف الهيكلي

- حذف ملفات ميتة: `AIRouterEngine.ts`, `ModelValidatorService.ts`, `apiValidation.ts`, `ApiKeyValidatorInput.tsx` (نسختان), `ModelPickerWithDots.tsx`, `types/ProviderHealthTypes.ts`, و5 سكربتات `test-cors*.js` / `test-fetch*.js` (تحقق منها أن `fetch` الأصلي كافٍ — لم تعد هناك حاجة لها).
- إزالة props/حقول أنواع غير مستهلكة في: `ChatPage`, `UserBubble`, `Header`, `ArtifactsPanel`, `LivePreview`, `WelcomeView`, `ToolCall`, `MessageAttachment`, `AppSettings` (7 مفاتيح مكررة للـ proxy)، `PremiumAvatar`.
- `src/version.ts` صار مصدر الحقيقة الوحيد للاسم/الإصدار/مفاتيح التخزين، مع ترحيل تلقائي من المفاتيح القديمة (`gbackground_pro_*`, `GBeast`, `nvidia_api_key` → `gbai_*_v15`) في `main.tsx`. **اسم قاعدة بيانات Dexie `GSoul_Beast_v14` تُرك عمداً** لأن تغييره يفصل المستخدمين عن ذواكرهم — التوسعة يجب أن تتم عبر `version(2).stores(...)`، ووثّقت ذلك بتعليق في `GSoulEngine`.
- إصلاح تعليقات `vite.config.ts` التي كانت تالفة الترميز (mojibake) وإزالة `@license-id: MIT-0` الكاذبة من رأس الملفات.
- **87 → 0** خطأ في `lint:strict`.

---

## 7) خطة التطوير المقترحة (مرتّبة بالأولوية)

### أسبوع 1 — أمان وصدق النتائج
1. **تأكيد بشري للأدوات المؤثرة**: نافذة موافقة قبل `exec_js`/`run_python`/`termux_*`/`run_sql` (قائمة `requiresApproval` في `TOOL_META` جاهزة). يمنع تنفيذ شيفرة ناتجة عن prompt injection من صفحة ويب.
2. **كشف تسرّب المفاتيح**: مسح قيم `apiKey`/`serper` من أي نص يدخل سياق النموذج أو ناتج أداة (كان `context_analyzer` يفكّ base64 لكل `localStorage` ويعرض المفاتيح في الدردشة — حُذف).
3. **إصلاح الأدوات «شبه الحقيقية» فعلياً بدل وسمها**: `data_analyst` و`pdf_analyzer` (Pyodide/pandas وpypdf)، `image_analysis` (رؤية النموذج بدل «محاولة فهم» وهمية)، `system_monitor` (`performance.memory` + `navigator.deviceMemory`…).

### أسبوع 2 — جودة المحادثة
4. **تحرير/إعادة توليد للرسائل** مع فرع جلسة (تعديل `content` + `contentHtml` غير مستخدمين حالياً) — إعادة التوليد من أي نقطة.
5. **معالجة صور حقيقية**: مرفقات الصور اليوم تُعرض فقط؛ يجب تحويلها `image_url` part للنماذج البصرية مع ضغط قبل الإرسال.
6. **إعادة الاتصال والتكملة** (resume/stream retry): عند انقطاع التدفق، إكمال نفس الرسالة بدل فقدها، مع «retry» على الفقاعة.
7. **اختيار مزود تلقائي ذكي**: `detectTask` موجودة وتعمل؛ اجعل وضع `auto` يختار المزود حسب التكلفة/السرعة/توفر المفتاح بدل النموذج الافتراضي، مع بانل يشرح سبب الاختيار.

### أسبوع 3 — الذاكرة والملفات
8. **بحث دلالي في GSoul**: فهرسة embeddings عبر المزود + `sqlite-vec`/تشابه جيب التمام بدل البحث النصي الحالي (`semantic.where(...)`).
9. **ملاحظات قابلة للتحرير لكل رسالة** (pin/edit) لتغذية `semantic` بدل الاعتماد على `remember`.
10. **Workspace فعلي**: استبدال حالة `projectFiles` بواجهة تخزين على القرص (File System Access API مع fallback لـ IndexedDB) حتى تُحفظ ملفات المشاريع الكبيرة بلا ضغط على localStorage.

### أسبوع 4 — البنية
11. **تقسيم `App.tsx`** (1800 سطر) إلى hooks: `useSessions`, `useChatRequest`, `useAgentRun`, `useProviders` — مع إبقاء `App.tsx` كـ composition root فقط. (كل المنطق الجديد كُتب بأسلوب قابل للنقل.)
12. **طبقة تخزين واحدة**: نقل الرسائل من `localStorage` إلى Dexie (نفس قاعدة GSoul) لإزالة حدّ الحصة نهائياً، مع إبقاء `safeStorage` للإعدادات.
13. **CI**: `npm run lint:strict && npm test && npm run smoke && npm run build` على كل دفع + قياس حجم الحزمة لمنع الانتكاس (regression) التضخم.
14. **Playwright** لثلاثة سيناريوهات: بدء جلسة + إرسال + تدفق الرد، تفعيل Agent + استدعاء أداة، والتصدير/الاستيراد.

### إضافات ذات قيمة عالية/جهد منخفض
- تصدير/استيراد **جلسة مفردة** بصيغة Markdown/JSON للنسخ الخارجي (يوجد ZIP كامل فقط).
- `Ctrl/Cmd+Enter` لإرسال، `↑` لتحرير آخر رسالة (جزء منه موجود في `handleArrowUp` لكنه لا يملأ الحقل دائماً).
- «إعداد سريع» لأول تشغيل: يكتشف المفاتيح من `VITE_*` أو من المتغير البيئي ويطلب واحداً فقط بدل فتح صفحة المزودين.
- وسم استهلاك التوكنز لكل رسالة (تقديري) بدل الشريط الكلي فقط — يساعد ضبط `ctx`/`maxTok`.

---

## 8) ما لم أستطع التحقق منه هنا

- **لم أختبر طلباً حقيقياً على مزود** (لا مفاتيح في هذه البيئة): الاختبارات تغطي شكل الطلب والتحليل، لكن **يجب** تجربة مفتاح Gemini + NVIDIA + مزود مخصص مرة واحدة قبل الاعتماد.
- **Pyodide وWeb Workers** لا يعملان في Node؛ منطقهما مُختبَر فقط عبر الـ smoke render (التركيب) — يحتاج تجربة نقر في المتصفح.
- **بناء Capacitor/APK** لم يُجرَّب (لا Android SDK في البيئة).
- الأدوات المحاكاة تُركت كما هي **من حيث التنفيذ** لأنها لا يمكن أن «تعمل» بدون عتاد فعلي؛ عولجت بالصدق والحظر، لا بالتزوير.
